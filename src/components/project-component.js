import React from "react"
import { Link } from "gatsby"

const Project = ({title, description}) => (
    <div>
        <h3>{title}</h3>
        <h4>{description}</h4>
    </div>
)

export default Project